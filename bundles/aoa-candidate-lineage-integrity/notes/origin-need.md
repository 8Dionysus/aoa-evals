# Origin Need

Growth-refinery rollout now spans checkpoint carry, reviewed candidate identity,
seed staging, and downstream owner proof surfaces.

Without one bounded lineage-integrity eval, later stats, proof, and writeback
layers can overread a chain that only looks coherent on the surface.
