# Eval integrity check

This bundle stays bounded only if:

- the report schema remains smaller than the protocol claim
- the example report stays weaker than owner-local truth
- the wording never implies runtime, certification, or governance authority
