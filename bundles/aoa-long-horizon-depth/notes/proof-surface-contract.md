# Proof Surface Contract

This note records the machine-readable proof surface for
`aoa-long-horizon-depth`.

The draft bundle remains narrow:
- it is about checkpoint-based restart fidelity on long-horizon inquiry routes
- it is not final-answer grading
- it is not broad long-horizon capability scoring
- it is not a replacement for `aoa-return-anchor-integrity`

The materialized proof flow keeps these surfaces explicit:
- shared case-family pressure in `fixtures/long-horizon-restart-v1/README.md`
- bounded local replacement rules in `fixtures/contract.json`
- reportable runner inputs in `runners/contract.json`
- schema-backed restart readout in `reports/summary.schema.json`
- a concrete companion example in `reports/example-report.json`

The machine-readable surface should preserve:
- checkpoint completeness as a visible review object
- the active axis and contradiction posture through relaunch
- `memory_delta` versus `canon_delta` separation
- false-continuity pressure as a first-class downgrade signal
