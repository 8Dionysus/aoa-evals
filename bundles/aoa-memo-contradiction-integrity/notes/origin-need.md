# Origin Need

`aoa-memo-contradiction-integrity` exists because lifecycle-aware memo
consumption can still flatten contradiction even when recall precision and
compactness look disciplined.

This bundle tests the smaller claim before the wider memo guardrail program is
expanded into permission, promotion, or merge lanes.
