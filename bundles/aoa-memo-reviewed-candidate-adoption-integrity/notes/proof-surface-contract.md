# Proof Surface Contract

This note records the machine-readable proof surface for
`aoa-memo-reviewed-candidate-adoption-integrity`.

The draft bundle remains narrow:

- it is about reviewed-candidate adoption on runtime distillation paths
- it is not confirmed memo-surviving-event writeback
- it is not contradiction handling
- it is not reviewed-candidate promotion or final bridge lift
- it is not permission inference from memo fields
- it is not future scar or retention proof
- it is not live memory-ledger behavior

The materialized proof flow keeps these surfaces explicit:

- shared case-family pressure in
  `fixtures/memo-reviewed-candidate-adoption-guardrail-v1/README.md`
- bounded local replacement rules in `fixtures/contract.json`
- reportable runner inputs in `runners/contract.json`
- schema-backed adoption readout in `reports/summary.schema.json`
- a concrete companion example in `reports/example-report.json`
- a current gap-shaped readout in
  `reports/current-reviewed-candidate-adoption-gap.report.json`
- the already-landed base writeback lane in
  `bundles/aoa-memo-writeback-act-integrity/`

The machine-readable surface should preserve:

- runtime target mapping integrity
- reviewed-to-adopted linkage
- live receipt visibility or the lack of it
- candidate-posture honesty on recall surfaces
- asymmetric current support across claim, pattern, and bridge legs without
  widening the claim
