# Proof Surface Contract

This note records the machine-readable proof surface for
`aoa-trace-outcome-separation`.

The bounded bundle remains narrow:
- it is about separate outcome and path readings on bounded change workflows
- it is not the narrower tool-trajectory bundle
- it is not an artifact-versus-process bridge

The materialized proof flow keeps these surfaces explicit:
- shared case-family pressure in `fixtures/trace-outcome-bounded-v1/README.md`
- bounded local replacement rules in `fixtures/contract.json`
- reportable runner inputs in `runners/contract.json`
- schema-backed split readout in `reports/summary.schema.json`
- a concrete companion example in `reports/example-report.json`

The machine-readable surface should preserve:
- separate outcome and path readings before any combined note
- the combined reading as a downstream judgment
- failure-versus-readout separation on every case
