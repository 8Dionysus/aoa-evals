# Artifact Process Separation Guide

This guide defines how the current artifact/process layer should be read as one bounded program rather than as three unrelated bundle pages.

Use it when the question is:
- which surface do I read first: artifact, workflow, or bridge?
- what keeps artifact polish from collapsing into workflow proof?
- where do witness and compost pilots sit relative to artifact/process reading?

## Read order

Read the current artifact/process layer in this order:
1. `aoa-artifact-review-rubric` for the artifact-side reading
2. `aoa-bounded-change-quality` for the process-side reading
3. `aoa-output-vs-process-gap` for the bridge verdict under matched conditions
4. `aoa-witness-trace-integrity` only as adjacent upstream witness context when needed
5. `aoa-compost-provenance-preservation` only as adjacent downstream artifact context when needed

The bridge does not replace the standalone surfaces.
It reports divergence or alignment only after both standalone readings exist.

## Shared comparison vocabulary

Keep these terms stable across the layer:
- `artifact-side reading`
- `process-side reading`
- `matched conditions`
- `shared case family`
- `bridge verdict`
- `style-over-substance risk`

The current materialized paired families are:
- `fixtures/bounded-change-paired-v1/README.md`
- `fixtures/bounded-change-paired-v2/README.md`

The current shared bridge dossiers are:
- `reports/artifact-process-paired-proof-flow-v1.md`
- `reports/artifact-process-paired-proof-flow-v2.md`

## Anti-collapse rules

Do not read this layer as:
- artifact polish proving workflow discipline
- workflow discipline proving artifact excellence
- witness integrity replacing workflow review
- compost provenance replacing artifact review
- one bridge verdict replacing the standalone artifact or workflow evidence

Default honesty rules:
- artifact polish is weaker than workflow proof
- workflow discipline is weaker than artifact excellence
- a mixed paired case should stay mixed
- side-by-side readouts should name matched-condition evidence directly, not stylistic impression alone

## Status discipline

The current public posture remains:
- `aoa-artifact-review-rubric` is `portable`
- `aoa-bounded-change-quality` is `portable`
- `aoa-output-vs-process-gap` is `draft`
- `aoa-witness-trace-integrity` is `draft`, but now ships a materialized witness-facing proof flow
- `aoa-compost-provenance-preservation` is `draft`, but now ships a materialized provenance-preserving proof flow

This means the bridge still stays weaker than the two standalone starter surfaces.
