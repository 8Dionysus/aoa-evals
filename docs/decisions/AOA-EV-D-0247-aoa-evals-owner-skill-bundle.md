# aoa-evals Owner Skill Bundle

- Decision ID: AOA-EV-D-0247
- Status: Accepted
- Date: 2026-07-16
- Owner surface: `skills/aoa-evals/SKILL.md` and `skills/port.manifest.json`

## Index Metadata

- Original date: 2026-07-16
- Surface classes: agent-route, root/topology, owner-boundary, proof-topology
- Mechanic parents: none
- Guard families: repository skill admission, projection ownership, source/topology
- Posture: accepted owner-skill admission

## Context

AOA-EV-D-0246 removed 25 copied shared bundles and required any future
repository skill to earn an independent trigger, ABI, composition value,
coexistence behavior, and benefit over no skill. Manual work then compared
ordinary repository navigation with one provisional `aoa-evals` owner bundle.

Without the candidate, bounded questions could still be answered, but the
agent repeatedly reconstructed the same authority path across the chooser,
source bundle, Eval Forge, review guidance, dashboards, reports, and generated
readers. The candidate made that central-owner seam explicit and consistently
returned the source-first proof ceiling and a bounded disposition ABI.

The trials covered exact selection, readiness overread, a no-fit Eval Forge
handoff, unsupported maturity promotion, controlled generated-reader drift,
and a held-out owner-fit-versus-scope-drift selection. Negative cases covered
ordinary tests, an already selected local eval, generic analytics, raw session
retrieval, and decision lookup. Coexistence checks kept cross-repo selection
and local application with shared `aoa-eval`, durable rationale with
`aoa-decision`, and raw evidence retrieval with session-memory routing.

These trials support the owner-specific procedure and held-out transfer. They
do not establish token or latency improvement, cross-model or cross-host
parity, security, or general outcome superiority.

## Options Considered

- Keep no repository skill and reconstruct the central proof route each time.
- Add the procedure to shared `aoa-skills` or expand shared `aoa-eval` until it
  owns central proof interpretation.
- Advertise separate selection, review, and evolution skills.
- Admit one owner-local bundle with three internal modes and one managed
  OS-level user projection.

## Decision

Admit one repository-owned `aoa-evals` bundle with internal `select`, `review`,
and `evolve` modes. Canonical source lives at `skills/aoa-evals/SKILL.md`.
`skills/port.manifest.json` declares version, lifecycle, visibility, admission,
and selection by the central `os-user-default` profile. The managed user copy
is a delivery projection; `.agents/` carries no duplicate skill package.

The bundle owns a callable route through central bounded-proof work, not proof
meaning itself. Source `EVAL.md`, `eval.yaml`, and admitted evidence remain
stronger than the skill, reports, readiness surfaces, receipts, generated
readers, MCP, KAG, and validation results.

Keep the three modes together until later held-out work proves independent
prompt-visible value. Do not require technique packages to invoke the skill.
Technique references inside an eval remain local lineage or support to inspect,
not inherited procedure dependencies.

## Rationale

The owner bundle reduces repeated route reconstruction while preserving the
boundary shared `aoa-eval` cannot own: central proof interpretation and owner
admission. One bundle limits prompt competition, shares a typed output ABI, and
lets a task assemble only the needed `select -> review -> evolve` edges rather
than forcing a fixed workflow.

The common `aoa-skills` home-port contract protects the durable source and
projection invariant. Manual trials, not that validator, justify admission.

## Consequences

- Positive: a fresh repository session discovers one evals-specific owner
  capability instead of copied shared bundles or no central front door.
- Positive: source/projection ownership, typed relations, effects, handoffs,
  failure dispositions, and health triggers are explicit.
- Positive: shared `aoa-eval`, decision, session-memory, stats, runtime, and
  proof owners keep their narrower authority.
- Tradeoff: one additional prompt-visible description consumes catalog space.
- Tradeoff: material model, workflow, or bundle changes require new manual
  routing and outcome review before lifecycle expansion.
- Follow-up: KAG may index the owner source, typed relations, and install
  provenance, but must keep the managed user copy a derived projection rather
  than a second authored skill.

## Current Applicability

As of 2026-07-17:

- Still valid: one bundle with three internal modes is the admitted owner
  route; manual evidence across `select`, `review`, `evolve`, negative
  applicability, and coexistence, not projection validation, justifies
  admission on the exercised Codex host and model. Version `0.1.18` is the
  current owner package and preserves the three-mode contract while adding the
  exact packet-ID-only review route described below.
- Changed: `aoa-evals` now has one repository skill home after the absence
  state recorded by AOA-EV-D-0246. Its Codex exposure is now selected once by
  the OS-level `os-user-default` profile rather than copied into this repo.
- Superseded by: the repo-scoped projection portion of this decision is
  superseded by the v2 owner-home exposure contract.

## Review Log

### 2026-07-18 - Move active exposure to the single OS user profile

- Previous assumption: owner admission implied one exact repository projection
  under `.agents/skills`.
- New reality: the central v2 exposure contract selects `aoa-evals` once in
  `os-user-default`; the repository retains only canonical `skills/` source and
  maintained `.agents/` lanes.
- Reason: neutral sessions need global owner-function discovery, while duplicate
  user and repository names create routing ambiguity.
- Claim limit: structural profile checks prove delivery identity only. Manual
  fresh-session work remains required for prompt visibility, selection,
  coexistence, and outcome claims.

### 2026-07-16 - Split the admitted bundle by progressive disclosure

- Previous assumption: one `SKILL.md` could carry source return, the shared
  contract, and all three complete mode procedures without material selection
  cost.
- Observed pressure: a fresh central-selection trial reached the right source
  bundle only after loading the 299-line front door and consuming 130,611
  tokens; later routing also exposed generic query terms and excess neighbor
  loading.
- Decision: keep one advertised `aoa-evals` identity, but make its 70-line
  `SKILL.md` select exactly one conditional mode. Move the shared ABI,
  source-return law, and full `select`, `review`, and `evolve` procedures into
  direct one-level references. Use the bundled read-only packet resource to
  validate the installed source receipt or canonical owner home and return
  bounded catalog/source-contract packets.
- Manual result: the same bounded selection class on v0.1.9 used one shortlist
  and exactly two source contracts, selected
  `aoa-trace-outcome-separation`, rejected `aoa-verification-honesty` as the
  primary, preserved the proof ceiling, and made no changes. The raw session
  total was 87,391 tokens versus 130,611 before the split, a reduction of about
  33 percent for this one exercised selection class. The previously recorded
  21,983 figure was an incremental counter, not total session usage, and was
  therefore removed.
- Claim limit: this supports the progressive-disclosure structure and this
  owner-source selection route on the exercised Codex host/model only. It does
  not yet prove `review`, `evolve`, coexistence, cross-model behavior, or
  general token superiority.

### 2026-07-16 - Make owner-source return explicit for review and evolve

- Observed defect: the first fresh `review` trial reached the correct
  `candidate_only` conclusion, but `source-return.md` advertised a generic
  `<mode>` command while the helper exposed only `catalog` and `contracts`.
  The agent probed help, widened into repository search, and consumed 297,246
  tokens. A correct final answer did not make that procedure acceptable.
- Correction: v0.1.10 adds one read-only `source` packet for `review` and
  `evolve`; `select` continues to use `catalog` and `contracts`, which already
  return the same owner provenance. The mode references now forbid catalog
  search for an already named proof object and give the exact Eval Forge
  readiness route.
- Follow-up trial: source return then worked exactly once and the correct
  `candidate_only` ceiling was preserved, but the procedure still ran the
  readiness gate, candidate validator, and Forge route for a contract-level
  authority question. v0.1.11 therefore separates reading a readiness contract
  from executing its operational commands and bounds `session:*` packet lookup
  to the candidate directory.
- Claim limit: this corrects the discovered source-return ABI. Fresh review,
  evolve, negative, and coexistence trials remain required before the
  correction earns a behavioral verdict.

### 2026-07-17 - Bound review expansion after the disposition is fixed

- Previous assumption: exact source return plus a ban on operational readiness
  commands was enough to make the review route compact.
- Observed reality: the v0.1.11 fresh review returned the correct
  `candidate_only` ceiling and ran no readiness command, validator, generator,
  or test, but it still enumerated route cards, read the ancestor card chain,
  inspected neighboring reports and generated readers, and reread sources for
  line citations. The final raw session total was 284,612 tokens across 31 tool
  calls. A previously sampled 71,364 counter was incremental rather than the
  final session total and is not valid review-cost evidence.
- Correction: v0.1.12 reads only the root owner card plus the nearest material
  nested card for each exact supplied path, reads exact documents with line
  numbers once when citations are needed, forbids unrelated source-bundle and
  neighboring-reader searches, and stops expanding weaker surfaces as soon as
  a missing gate makes every higher disposition impossible. `evolve` now stops
  at failed entry gates before comparison, manual case suites, validation, or
  source drafting.
- Validation: fresh review, evolve, negative, and coexistence trials remain
  required. This amendment records the observed failure and candidate
  correction; it does not claim that v0.1.12 is cheaper or behaviorally
  admitted before those trials.

### 2026-07-17 - Replace review-path guessing with one exact context packet

- Observed result: v0.1.12 again returned the correct `candidate_only` ceiling
  without running readiness or validation commands, and reduced tool calls
  from 31 to 24. It still consumed 277,415 total tokens because the agent
  inspected the neutral workspace before source return, probed nonexistent
  `skills/aoa-evals/EVAL.md` paths, enumerated route cards, read ancestor edit
  cards, and searched twice for the exact packet.
- Correction: v0.1.13 makes the bundled owner route the first navigation
  action and adds `review-context` for an exact owner-review report plus packet
  ID. The resource resolves one owner, one report, one exact packet, the
  readiness contract, their digests, direct candidate route fields, and
  explicit source-bundle-link posture. The procedure reads those source files
  directly and does not treat read-only evidence inspection as an edit-route
  card traversal.
- Manual result: a fresh independent review selected `review`, ran
  `review-context` once, read only the root owner card plus the three returned
  material files, and returned the correct `candidate_only` ceiling. It ran no
  readiness command, validator, generator, test, repository search, guessed
  path probe, or nested route-card traversal. The final raw session total was
  93,954 tokens across 9 tool calls, versus 277,415 tokens and 24 calls for the
  same review class on v0.1.12.
- Claim limit: this establishes a compact exact-report-plus-packet review route
  on the exercised Codex host and model. The packet supplies navigation and
  copied route fields, not proof meaning or a verdict. `evolve`, negative
  applicability, coexistence, cross-model behavior, and general cost
  superiority remain unproved.

### 2026-07-17 - Select owner evolution before evaluating its gates

- Observed defect: a request to convert a named `session:` candidate into a
  central proof bundle while forbidding changes selected `review` because
  accepted owner review was absent. It returned an honest no-change result, but
  treated gate satisfaction as mode selection and then loaded `.aoa`
  session-memory guidance solely because the candidate ID began with
  `session:`. The run consumed 196,740 total tokens across 9 tool calls without
  exercising the requested `evolve` gate.
- Correction: v0.1.14 selects the mode from the requested operation. A request
  to add, promote, supersede, retire, or otherwise change owner proof selects
  `evolve`; missing review, authority, or confirmation then blocks that mode.
  A `session:` eval-candidate ID remains an owner-routed proof object and does
  not by itself trigger session-memory loading.
- Manual result: a fresh trial selected `evolve`, resolved owner source once,
  read the root owner card, returned `blocked` for the missing review,
  acceptance criteria, effect authority, and human confirmation, and loaded no
  session-memory guidance. It stopped before artifact comparison, drafting,
  validation, or source change. The final raw total was 85,770 tokens across 6
  tool calls.
- Remaining defect and correction: the first command combined the initial
  `SKILL.md` read with `pwd` and `rg --files`, before the body prohibition could
  govern the next action. v0.1.15 moves the package-before-workspace rule into
  the always-visible description. A fresh trial must confirm that this removes
  pre-source workspace inspection.
- Claim limit: the exercised v0.1.14 route supports correct mode selection,
  gate termination, and session-memory coexistence on one host/model scenario.
  Negative applicability, broader coexistence, and v0.1.15 pre-search behavior
  remain unproved.

### 2026-07-17 - Confirm the v0.1.15 package-first route and owner-family boundary

- Package-first correction: a fresh `evolve` trial loaded the selected package
  before workspace inspection, chose `evolve` from the requested owner change,
  resolved owner source once, loaded no session-memory guidance, performed no
  pre-source search, and stopped on missing review, acceptance criteria, effect
  authority, and human confirmation. It made no change. The final raw total
  was 108,346 tokens across 8 tool calls.
- Review repeat: a fresh exact report-plus-packet review selected `review`, ran
  `review-context` once, read only the root owner card and three material
  returned files, and preserved the `candidate_only` proof ceiling. It ran no
  readiness command, validator, generator, test, repository search, guessed
  path probe, or nested route-card traversal. The final raw total was 94,671
  tokens across 9 tool calls.
- Negative applicability: an ordinary request to run one already named local
  test loaded no eval skill and executed only the requested command. The test
  passed, but that green result is execution evidence for the test, not
  evidence of skill benefit. The final raw total was 27,971 tokens across one
  tool call.
- Coexistence: an exact decision-rationale request selected `aoa-decision`
  alone and did not load `aoa-evals`, supporting the owner-family negative
  boundary for that wording. The selected decision route itself widened into
  repository search and consumed 227,423 tokens across 13 tool calls. That is
  a separate `aoa-decision` route defect and means this trial cannot establish
  full-profile cost superiority.
- Verdict and limit: v0.1.15 is behaviorally admitted for the exercised
  `select`, `review`, `evolve`, negative, and coexistence cases on this Codex
  host and model. This does not prove every wording, general token or latency
  superiority, cross-model or cross-host parity, security, or the efficiency
  of neighboring skills. Recheck after a material model, host, workflow,
  source-bundle, or skill change.

### 2026-07-17 - Resolve a named candidate without a supplied report path

- Observed defect: a natural central-proof review supplied only the exact
  `session:` candidate ID. Versions `0.1.16` and `0.1.17` selected
  `aoa-evals`, but the first command combined the package read with workspace
  orientation and broad search. The source-return helper could resolve an
  exact report only when the caller already knew its path, so the procedure
  failed the package-before-workspace gate even though its final proof ceiling
  was correct.
- Correction: `review-context` now accepts a packet ID without a report path.
  It validates the owner source, resolves exactly one candidate packet, derives
  exactly one owner-relative `*.manual-review.md` from that packet's evidence
  references, and returns the readiness contract plus provenance and digests.
  An explicit report remains supported and is labeled separately. Version
  `0.1.18` also places the first-tool-turn requirement at the beginning of the
  host-visible description, where selection-time truncation cannot hide it.
- Manual result: a fresh full-profile trial read only `aoa-evals/SKILL.md` on
  its first tool turn, loaded the shared contract, `review`, and source-return
  references, then invoked `review-context` once with only the packet ID. It
  read the root owner card and the three exact returned material files,
  preserved `candidate_only`, and stopped. It ran no readiness command,
  validator, generator, test, broad search, guessed path probe, or unrelated
  skill. The result used 9 read-only commands and 108,544 raw input tokens.
- Coexistence result: the same request was adjacent to statistical evidence
  language but selected `aoa-evals` alone; `aoa-stats` did not load. A
  `session:` prefix on an owner-routed eval object also did not activate
  session-memory guidance.
- Resource boundary: the bundled helper is deterministic navigation, not a
  proof evaluator or validator. It may return source paths, copied route
  fields, provenance, and digests; source records still own meaning and the
  agent still performs the bounded review.
- Verdict and limit: `0.1.18` supersedes `0.1.15` only for the natural
  packet-ID review and package-first routing correction. The broader
  `select`, `review`, `evolve`, negative, and coexistence admission remains the
  bounded `0.1.15` evidence above. This amendment does not prove every packet
  layout, cross-model or cross-host behavior, security, or general token and
  latency superiority.

### 2026-07-16 - Move discovery to the single OS user profile

- Previous assumption: every admitted owner home needed an exact repository
  copy under `.agents/skills`.
- New reality: the owner package remains canonical under `skills/`; one
  managed user-profile copy supplies global discovery without duplicate names.
- Reason: a new agent must discover owner functionality before entering its
  repository, while Codex does not reliably deduplicate the same canonical
  bundle from user and repository roots.
- Source surfaces updated: `skills/port.manifest.json`, `skills/README.md`,
  `skills/AGENTS.md`, `.agents/AGENTS.md`, and `DESIGN.md`.
- Validation: manual OS-profile installation and fresh-session discovery are
  required before closeout; source checks prove only package identity.

## Boundaries

This decision does not admit a new eval bundle, change any proof claim,
category, maturity, baseline, report, or verdict, or turn the skill into proof
authority. It does not make manual session traces repository truth, require
siblings to create skill homes, or prove cross-model, cross-host, security,
latency, or token behavior.

## Validation

Admission uses the manually exercised cases summarized above, portable skill
shape review, exact home-port source/projection validation, fresh prompt-visible
inspection, decision-index parity, repo-local KAG regeneration, repository
validation, CI, and post-merge inspection. Structural checks protect only the
durable shape they actually cover.
