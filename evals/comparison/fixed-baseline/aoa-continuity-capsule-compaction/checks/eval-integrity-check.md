# Eval Integrity Check

Checked:

- the bundle stays on a fixed-baseline field-preservation comparison
- the canonical capsule remains the named baseline target
- portable and private views retain exact identity and digest bindings
- protected-tail bytes are omitted from the portable report path
- report wording keeps synthetic contract checks separate from runtime proof,
  economy, promotion, and acceptance claims

Still baseline-limited:

- no real compaction or reinjection session is included
- no provider, model, host, transport, or semantic verdict is produced
- the D0 baseline gate remains outside this bundle

Nearby overread risk:

- treating a green field comparison as evidence that a live session retained
  its context or that validation cost improved
