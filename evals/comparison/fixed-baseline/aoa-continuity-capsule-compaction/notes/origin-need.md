# Origin Need

Direction 3 needs a bounded paired readout for the continuity capsule before
it can make any live compaction or economy claim.

The source work has three distinct owner boundaries: session-memory creates
the canonical capsule, the SDK carries its exact reference, and abyss-stack
reinforces the portable/private materialization at reinjection. A small eval
surface is needed to check field preservation across those boundaries without
copying raw session evidence into the eval repository.

This bundle opens that readout as a draft only. Baseline admission, runtime
execution, semantic acceptance, and owner acceptance remain separate.
