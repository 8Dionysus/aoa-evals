# Origin Need

`aoa-a2a-summon-return-checkpoint` exists because a reviewed child route can
look complete while silently smearing authority across summon contracts, SDK
helpers, checkpoint bridge notes, memo writeback, eval proof, and runtime
receipts.

The smaller claim is that one reviewed summon child-return route can preserve
those boundaries from request through dry-run closeout receipt.
