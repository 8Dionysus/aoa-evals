# Origin Need

`aoa-memo-recall-integrity` exists because the first live memo consumer wave
now has a real inspect/capsule/expand path, but that convenience can easily
hide over-expansion, provenance loss, or stale-memory overclaim.

This bundle tests the smaller claim before the wider memo guardrail program is
promoted downstream.
