# Example Report

## Bundle

- bundle: `aoa-memo-reviewed-candidate-adoption-integrity`
- bundle shape: `diagnostic`
- verdict: `does not support bounded claim`

This example answers the next memo-adoption question after the confirmed
writeback-act pilot:
does the current public seam keep reviewed distillation claim, pattern, and
bridge candidates inspectable from runtime mapping through memo adoption, live
receipt publication, and object-facing recall without turning candidate posture
into settled truth?
Its machine-readable companion lives at `reports/example-report.json`.

## Breakdown

- target mapping integrity: `strong`
- review to adoption alignment: `mixed`
- receipt visibility: `weak`
- candidate posture honesty: `mixed`
- recall surface alignment: `mixed`

## Strongest Signals

- strongest support: the runtime writeback seam already names all three reviewed-candidate target kinds and part of the adopted object family is visible on recall surfaces
- strongest risk: current public artifacts still let object visibility masquerade as completed adoption because live reviewed-candidate receipt coverage is missing

## Case Notes

| case id | read path | adoption reading | candidate note | review note | receipt note | recall note |
|---|---|---|---|---|---|---|
| RCA-CLAIM-01 | contract -> targets -> phase-alpha claim -> object recall family | mixed support | the claim leg already keeps distillation provenance visible | reviewed anchors exist, but not yet as one dedicated adoption lane | no reviewed-candidate live receipt is published | the claim remains recallable with explicit lifecycle posture |
| RCA-PATTERN-01 | contract -> phase alpha map -> recurrence pattern -> object recall family | does not support bounded claim | the seam names pattern adoption pressure | the current pattern is recurrence-shaped, not yet a clean distillation-adoption proof | no reviewed-candidate live receipt is published | recall exists, but recall alone is weaker than adoption proof |
| RCA-BRIDGE-01 | contract -> targets -> bridge candidate -> recall and donor export surfaces | mixed support | the bridge object already exists on candidate-facing surfaces | review posture stays proposed instead of pretending final lift | no reviewed-candidate live receipt is published | recall honesty is strong because candidate posture stays visible |

## Interpretation Boundary

This read supports only a bounded current-gap claim:
the reviewed-candidate adoption path is now explicit enough to inspect, and it
is also explicit enough to say that the current public seam still does not
fully support it across claim, pattern, and bridge.
It does not prove promotion readiness, contradiction handling, confirmed
writeback quality, graph-lift completion, live-ledger behavior, or general
memory quality.
