# Antifragility Distillation Log

## Slice 34

The active parent is `antifragility`. Former nearby parent names include
`antifragility-proof`, `stress-recovery`, and `repair`.

Distilled into active parts:

- first-wave posture support moved to `parts/posture-review/`;
- repeated-window stress recovery support moved to `parts/stress-recovery-window/`;
- bounded repair support moved to `parts/repair-proof/`.

Left in neighboring mechanics:

- comparison readout remains under `mechanics/comparison-spine/`;
- runtime-chaos selected evidence remains under `mechanics/audit/`.

Later routed:

- `aoa-diagnosis-cause-discipline` was later routed through
  `mechanics/growth-cycle/parts/diagnosis-gate/`; bounded repair proof remains
  under `parts/repair-proof/`.
