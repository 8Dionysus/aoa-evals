# Antifragility / Posture Review Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `posture-review` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/antifragility/parts/posture-review`.

## Commands

```bash
python scripts/validate_repo.py --eval aoa-antifragility-posture
```

Shared checks live in [VALIDATION.md — Non-mutating checks](../../../../VALIDATION.md#non-mutating-checks).
