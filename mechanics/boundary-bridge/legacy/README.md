# Boundary Bridge Legacy

This directory preserves old boundary-bridge path vocabulary behind the active
`boundary-bridge` mechanic.

Use [../PROVENANCE.md](../PROVENANCE.md) first. Use [INDEX.md](INDEX.md) for
old path lookup and [DISTILLATION_LOG.md](DISTILLATION_LOG.md) for landing
accounting.

New boundary-bridge work starts in the active part that owns the compatibility
map, latest sibling canary, orchestrator proof anchors, or Phase Alpha eval
matrix.

## Required Route

Open `../PROVENANCE.md` before local lookup. Use `INDEX.md` for
old-path lookup, `DISTILLATION_LOG.md` for raw-to-active accounting, and
`raw/README.md` for raw lineage.

This legacy directory is an archive-local route: it returns historical sources
to the current active route and keeps archive-local accounting in legacy.
Legacy is not active topology or a new-work entrypoint.
