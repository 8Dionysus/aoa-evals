# Boundary Bridge Legacy

This directory preserves old boundary-bridge path vocabulary behind the active
`boundary-bridge` mechanic.

Use [../PROVENANCE.md](../PROVENANCE.md) first. Use [INDEX.md](INDEX.md) for
old path lookup and [DISTILLATION_LOG.md](DISTILLATION_LOG.md) for landing
accounting.

Do not begin new boundary-bridge work here. New work starts in the active part
that owns the compatibility map, latest sibling canary, orchestrator proof
anchors, or Phase Alpha eval matrix.

## Required Route

Open `../PROVENANCE.md` before using this directory. Use `INDEX.md` for
old-path lookup, `DISTILLATION_LOG.md` for raw-to-active accounting, and
`raw/README.md` for raw lineage. Legacy is not active topology and not a
new-work entrypoint.
