# Boundary Bridge / Compatibility Map Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `compatibility-map` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/boundary-bridge/parts/compatibility-map`.

## Commands
The linked owner route retains `python scripts/validate_repo.py` for this check.

Shared checks live in [VALIDATION.md — Non-mutating checks](../../../../VALIDATION.md#non-mutating-checks).
