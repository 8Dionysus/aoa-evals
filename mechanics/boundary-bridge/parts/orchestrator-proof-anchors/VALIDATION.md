# Boundary Bridge / Orchestrator Proof Anchors Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `orchestrator-proof-anchors` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/boundary-bridge/parts/orchestrator-proof-anchors`.

## Commands
The linked owner route retains `python scripts/build_catalog.py --check` for this check.

Shared checks live in [VALIDATION.md — Non-mutating checks](../../../../VALIDATION.md#non-mutating-checks).
