# Comparison Spine / Spine Overview Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `spine-overview` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/comparison-spine/parts/spine-overview`.

## Commands

```bash
python scripts/build_catalog.py --check
python scripts/validate_repo.py
```
