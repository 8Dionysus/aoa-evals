# Distillation / Compost Provenance Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `compost-provenance` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/distillation/parts/compost-provenance`.

## Commands

```bash
python scripts/validate_repo.py --eval aoa-compost-provenance-preservation
```

Shared checks live in [VALIDATION.md — Non-mutating checks](../../../../VALIDATION.md#non-mutating-checks).
