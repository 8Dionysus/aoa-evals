# Distillation / Runtime Candidate Adoption Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `runtime-candidate-adoption` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/distillation/parts/runtime-candidate-adoption`.

## Commands

```bash
python scripts/validate_repo.py --eval aoa-memo-reviewed-candidate-adoption-integrity
```

Shared checks live in [VALIDATION.md — Non-mutating checks](../../../../VALIDATION.md#non-mutating-checks).
