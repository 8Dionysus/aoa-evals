# Experience / Governance Runtime Boundary Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `governance-runtime-boundary` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/experience/parts/governance-runtime-boundary`.

## Commands

```bash
python -m pytest -q mechanics/experience/parts/governance-runtime-boundary/tests
python scripts/validate_repo.py
```
