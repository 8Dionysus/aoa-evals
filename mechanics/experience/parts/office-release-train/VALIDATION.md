# Experience / Office Release Train Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `office-release-train` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/experience/parts/office-release-train`.

## Commands

```bash
python -m pytest -q mechanics/experience/parts/office-release-train/tests
python scripts/validate_repo.py
```
