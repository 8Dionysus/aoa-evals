# Method-growth Legacy

This legacy district preserves former root fixture placement and
growth-refinery path vocabulary behind the active method-growth route.

Start from `../README.md` and `../PARTS.md`. Enter legacy only when an old path,
generated reader, test expectation, or historical note needs provenance.

## Required Route

Open `../PROVENANCE.md` before using this directory. Use `INDEX.md` for
old-path lookup, `DISTILLATION_LOG.md` for raw-to-active accounting, and
`raw/README.md` for raw lineage. Legacy is not active topology and not a
new-work entrypoint.
