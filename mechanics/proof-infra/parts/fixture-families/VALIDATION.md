# Proof Infra / Fixture Families Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `fixture-families` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/proof-infra/parts/fixture-families`.

## Commands
The linked owner route retains `python scripts/build_catalog.py` for this check.

Shared checks live in [VALIDATION.md — Non-mutating checks](../../../../VALIDATION.md#non-mutating-checks).
