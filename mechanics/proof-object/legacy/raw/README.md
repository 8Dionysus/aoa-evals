# Raw Proof Object Legacy

No raw copied artifacts are stored here for the current proof-object contract
move.

The template and schemas were moved intact with `git mv`; provenance is kept
through git history, `legacy/INDEX.md`, and `legacy/DISTILLATION_LOG.md`.
