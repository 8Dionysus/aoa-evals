# Raw Questbook Legacy

No raw copied artifacts are stored here for the current questbook schema move.

The former root schema files were moved intact with `git mv`; provenance is
kept through git history, `legacy/INDEX.md`, and `legacy/DISTILLATION_LOG.md`.
