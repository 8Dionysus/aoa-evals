# Recurrence / Anchor Return Validation

Executable validation commands for this part are kept in this on-demand route.

Use the `anchor-return` child validation block there. This file is the part-local validation route marker so the README can remain a contract map.


Source anchor: `mechanics/recurrence/parts/anchor-return`.

## Commands

```bash
python scripts/validate_repo.py --eval aoa-return-anchor-integrity
python scripts/build_catalog.py --check
```
