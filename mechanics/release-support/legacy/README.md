# Release Support Legacy

This directory preserves lineage behind the active `release-support` mechanic.

Use [../PROVENANCE.md](../PROVENANCE.md) first. Use
[INDEX.md](INDEX.md) for old path lookup and
[DISTILLATION_LOG.md](DISTILLATION_LOG.md) for landing accounting.

Do not begin new release-support work here. New work starts in the active part
that owns the readiness, closeout, or PR handoff route.

## Required Route

Open `../PROVENANCE.md` before using this directory. Use `INDEX.md` for
old-path lookup, `DISTILLATION_LOG.md` for raw-to-active accounting, and
`raw/README.md` for raw lineage. Legacy is not active topology and not a
new-work entrypoint.
