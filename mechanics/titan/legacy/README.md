# Titan Legacy

This directory preserves former Titan canary placement and naming provenance
behind the active `mechanics/titan/` route.

Start from `../PROVENANCE.md`, then use:

- [INDEX.md](INDEX.md) for former path to active route mapping;
- [DISTILLATION_LOG.md](DISTILLATION_LOG.md) for raw-to-active accounting;
- [raw](raw/) for placement snapshots.

Legacy is not a home for new Titan canary work.

## Required Route

Open `../PROVENANCE.md` before using this directory. Use `INDEX.md` for
old-path lookup, `DISTILLATION_LOG.md` for raw-to-active accounting, and
`raw/README.md` for raw lineage. Legacy is not active topology and not a
new-work entrypoint.
