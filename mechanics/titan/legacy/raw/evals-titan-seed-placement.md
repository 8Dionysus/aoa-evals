# Former Titan Seed Placement Snapshot

Before the mechanics refactor, Titan seed canary YAML files lived under the
root `evals/` district.

Current active route:

`mechanics/titan/parts/seed-boundary/seeds/titan*.yaml`

This snapshot preserves the former placement shape. It is not the active seed
contract and does not list every seed as a separate active route.
